apiVersion: v1
kind: Service
metadata:
  name: {{ include "digital-id-verifier.fullname" . }}
  labels:
    {{- include "digital-id-verifier.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "digital-id-verifier.selectorLabels" . | nindent 4 }}
